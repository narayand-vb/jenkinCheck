node {
    checkout scm

    docker.withRegistry('https://registry.hub.docker.com', 'dockerCred') {

        def customImage = docker.build("firstreact")

        /* Push the container to the custom Registry */
        customImage.push()
    }
}