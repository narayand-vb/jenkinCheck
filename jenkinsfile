pipeline {
    agent any

    environment{
        CI = true
    }
    stages {
        stage('build') {
            steps {
                sh "docker compose up "
            }
        }

        stage('test') {
            steps {
                sh 'test successful'
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deployed"
            }
        }
        
    }
}